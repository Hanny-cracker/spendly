<div class="mx-auto max-w-2xl space-y-6"><div><a href="{{ route('categories',['type'=>$type]) }}" class="text-sm text-stone-500">&larr; Categories</a><h1 class="mt-4 text-2xl font-semibold">Edit Category</h1></div><form wire:submit="save" class="space-y-5 rounded-2xl border border-stone-200 bg-[#fbf8f2] p-6">@if($domainError)<p class="rounded-xl bg-red-50 p-3 text-sm text-red-700">{{ $domainError }}</p>@endif<label class="block">Name<input wire:model="name" class="mt-1 w-full rounded-xl border-stone-200">@error('name')<p class="text-xs text-red-600">{{ $message }}</p>@enderror</label><fieldset><legend class="mb-2">Type</legend><div class="grid grid-cols-2 gap-2">@foreach($types as $value=>$label)<label><input type="radio" wire:model="type" value="{{ $value }}" class="peer sr-only"><span class="block cursor-pointer rounded-xl border p-3 text-center peer-checked:border-emerald-700 peer-checked:bg-emerald-50">{{ $label }}</span></label>@endforeach</div></fieldset><label>Icon<select wire:model="icon" class="mt-1 w-full rounded-xl border-stone-200">@foreach($icons as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></label><fieldset><legend>Color</legend><div class="mt-2 flex gap-2">@foreach($colors as $value=>$label)<label><input type="radio" wire:model="color" value="{{ $value }}" class="peer sr-only"><span title="{{ $label }}" class="block h-9 w-9 rounded-full border-4 border-transparent peer-checked:border-stone-300" style="background:{{ $value }}"></span></label>@endforeach</div></fieldset><div class="flex justify-end gap-2 border-t pt-5"><a href="{{ route('categories',['type'=>$type]) }}" class="rounded-xl border px-4 py-2.5">Cancel</a><button wire:loading.attr="disabled" class="rounded-xl bg-emerald-700 px-4 py-2.5 font-semibold text-white">Update Category</button></div></form>
</div>
