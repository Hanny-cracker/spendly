<div class="space-y-7"><section class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="font-mono text-xs font-semibold uppercase text-emerald-700">Saving</p><h1 class="text-3xl font-semibold">Goals</h1><p class="text-sm text-stone-500">Save toward what matters to you.</p></div><a href="{{ route('goals.create') }}" class="rounded-xl bg-emerald-700 px-4 py-2.5 text-center text-sm font-semibold text-white">+ New Goal</a></section>
    <section class="grid grid-cols-2 gap-3 lg:grid-cols-4">@foreach([['Goal Target',$summary['target']],['Saved',$summary['saved']],['Remaining',$summary['remaining']]] as [$label,$value])<article class="rounded-2xl border border-stone-200 bg-[#fbf8f2] p-4"><p class="text-xs uppercase text-stone-500">{{ $label }}</p><p class="mt-3 font-mono text-xl font-bold">{{ number_format($value,0) }}</p><p class="text-xs text-stone-500">FCFA</p></article>@endforeach<article class="rounded-2xl border border-stone-200 bg-[#fbf8f2] p-4"><p class="text-xs uppercase text-stone-500">Active Goals</p><p class="mt-3 font-mono text-xl font-bold">{{ $summary['active_count'] }}</p></article></section>
    @if($goals->isEmpty())<section class="rounded-2xl border border-dashed border-stone-300 bg-[#fbf8f2] p-10 text-center"><h2 class="font-semibold">No savings goals yet</h2><p class="mt-1 text-sm text-stone-500">Create a goal and start saving toward something that matters.</p><a href="{{ route('goals.create') }}" class="mt-5 inline-flex rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white">Create your first goal</a></section>
    @else<section class="grid gap-4 lg:grid-cols-2">@foreach($goals as $goal)@php($completed=$goal['status']==='completed')@php($overdue=$goal['is_overdue'])<article class="rounded-2xl border border-stone-200 bg-[#fbf8f2] p-5"><div class="flex justify-between gap-3"><a href="{{ route('goals.show', $goal['public_id']) }}" class="font-semibold hover:text-emerald-700">{{ $goal['name'] }}</a><span class="rounded-full px-2.5 py-1 text-[10px] font-semibold {{ $completed?'bg-emerald-50 text-emerald-700':($overdue?'bg-amber-50 text-amber-700':'bg-stone-100 text-stone-600') }}">{{ $completed?'Completed':($overdue?'Overdue':'Active') }}</span></div>@if($goal['description'])<p class="mt-1 text-xs text-stone-500">{{ $goal['description'] }}</p>@endif<p class="mt-5 font-mono text-lg font-bold">{{ number_format($goal['current_amount'],0) }} <span class="text-xs font-normal text-stone-500">of {{ number_format($goal['target_amount'],0) }} FCFA</span></p><div class="mt-3 h-2 overflow-hidden rounded-full bg-stone-200"><div class="h-full rounded-full {{ $completed?'bg-emerald-600':($overdue?'bg-amber-600':'bg-emerald-700') }}" style="width:{{ min(100,max(0,$goal['percentage'])) }}%"></div></div><div class="mt-3 flex justify-between text-xs"><span class="font-mono font-bold">{{ number_format($goal['percentage'],1) }}%</span><span class="text-stone-500">{{ $completed?'Goal reached ✓':number_format($goal['remaining_amount'],0).' FCFA remaining' }}</span></div><div class="mt-4 flex items-center justify-between"><p class="text-xs text-stone-500">{{ $goal['target_date'] ? 'Target: '.$goal['target_date'] : 'No target date' }}</p><div class="flex gap-3 text-xs font-semibold"><a href="{{ route('goals.show', $goal['public_id']) }}" class="text-emerald-700">View</a><a href="{{ route('goals.edit', $goal['public_id']) }}" class="text-stone-600">Edit</a></div></div></article>@endforeach</section>@endif
</div>
