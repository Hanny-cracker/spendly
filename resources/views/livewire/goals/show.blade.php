<div class="mx-auto max-w-4xl space-y-7 transition-opacity" wire:loading.class="opacity-60">
    <section><a href="{{ route('goals') }}" class="text-sm font-medium text-stone-500 hover:text-emerald-700">&larr; Goals</a><div class="mt-4 flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><h1 class="text-3xl font-semibold">{{ $progress['name'] }}</h1>@if($progress['description'])<p class="mt-1 text-sm text-stone-500">{{ $progress['description'] }}</p>@endif</div><div class="flex gap-2"><a href="{{ route('goals.edit', $goal) }}" class="rounded-xl border border-stone-300 px-4 py-2.5 text-sm font-semibold">Edit</a><div class="relative" x-data="{ open:false }" @click.outside="open=false"><button type="button" @click="open=!open" class="h-10 rounded-xl border border-stone-300 px-3" aria-label="Goal actions">•••</button><div x-cloak x-show="open" class="absolute right-0 z-30 mt-2 w-40 rounded-xl border border-stone-200 bg-[#fbf8f2] p-1 shadow-lg"><button type="button" wire:click="$set('confirmingGoalDeletion', true)" @click="open=false" class="w-full rounded-lg px-3 py-2 text-left text-sm font-medium text-red-700 hover:bg-red-50">Delete goal</button></div></div></div></div></section>
    @if($successMessage)<div class="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ $successMessage }}</div>@endif
    <section class="rounded-2xl border border-stone-200 bg-[#fbf8f2] p-6 sm:p-8"><div class="flex justify-between gap-4"><p class="font-mono text-xs font-semibold uppercase tracking-[0.18em] text-emerald-700">{{ $progress['name'] }}</p><span class="rounded-full px-3 py-1 text-xs font-semibold {{ $progress['status']==='completed' ? 'bg-emerald-100 text-emerald-800' : ($progress['is_overdue'] ? 'bg-amber-100 text-amber-800' : 'bg-stone-100 text-stone-600') }}">{{ $progress['status']==='completed' ? 'Completed' : ($progress['is_overdue'] ? 'Overdue' : 'Active') }}</span></div><p class="mt-7 font-mono text-3xl font-bold sm:text-4xl">{{ number_format($progress['current_amount'], 0) }} <span class="text-sm font-normal text-stone-500">FCFA</span></p><p class="mt-1 text-sm text-stone-500">saved of {{ number_format($progress['target_amount'], 0) }} FCFA</p><div class="mt-6 h-3 overflow-hidden rounded-full bg-stone-200"><div class="h-full rounded-full bg-emerald-700" style="width:{{ min(100,max(0,$progress['percentage'])) }}%"></div></div><div class="mt-3 flex justify-between text-sm"><span class="font-mono font-bold">{{ number_format($progress['percentage'],1) }}%</span><span class="text-stone-500">{{ number_format($progress['remaining_amount'],0) }} FCFA remaining</span></div><div class="mt-7 flex flex-col gap-4 border-t pt-5 sm:flex-row sm:items-center sm:justify-between"><p class="text-sm text-stone-500">{{ $progress['target_date'] ? 'Target: '.$progress['target_date'] : 'No target date' }}</p>@if($progress['status']==='completed')<p class="font-semibold text-emerald-700">Goal reached ✓</p>@else<button type="button" wire:click="openContributionModal" class="rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white">+ Add Contribution</button>@endif</div></section>
    <section><div class="mb-4 flex items-center gap-3"><h2 class="font-mono text-xs font-semibold uppercase tracking-[0.18em] text-stone-500">Contribution History</h2><div class="flex-1 border-t border-dashed border-stone-300"></div></div><div class="overflow-hidden rounded-2xl border border-stone-200 bg-[#fbf8f2]">@forelse($contributions as $contribution)<article class="grid grid-cols-[1fr_auto] gap-1 border-b border-stone-200 px-5 py-4 last:border-0"><time class="text-xs text-stone-500">{{ $contribution->contributed_at->format('d M Y') }}</time><div class="flex items-center gap-2"><p class="font-mono text-sm font-bold text-emerald-700">+{{ number_format($contribution->amount,0) }} FCFA</p><div class="relative" x-data="{ open:false }" @click.outside="open=false"><button type="button" @click="open=!open" class="rounded-lg px-2 py-1 text-stone-500 hover:bg-stone-100" aria-label="Contribution actions">•••</button><div x-cloak x-show="open" class="absolute right-0 z-30 mt-1 w-32 rounded-xl border border-stone-200 bg-[#fbf8f2] p-1 shadow-lg"><button type="button" wire:click="editContribution({{ $contribution->id }})" @click="open=false" class="w-full rounded-lg px-3 py-2 text-left text-sm hover:bg-stone-100">Edit</button><div class="border-t"></div><button type="button" wire:click="confirmContributionRemoval({{ $contribution->id }})" @click="open=false" class="w-full rounded-lg px-3 py-2 text-left text-sm text-red-700 hover:bg-red-50">Remove</button></div></div></div><div class="text-sm text-stone-600">{{ $contribution->account?->name ?? 'Unassigned' }}@if($contribution->note)<p class="mt-1 text-xs text-stone-500">{{ $contribution->note }}</p>@endif</div></article>@empty<div class="px-6 py-10 text-center"><p class="font-semibold">No contributions yet</p><p class="mt-1 text-sm text-stone-500">Your goal savings history will appear here.</p></div>@endforelse</div>@if($contributions->hasPages())<div class="mt-5">{{ $contributions->links() }}</div>@endif</section>
    @if($showContributionModal)<div class="fixed inset-0 z-50 flex items-center justify-center bg-stone-950/40 p-4" role="dialog" aria-modal="true"><form wire:submit="{{ $editingContributionId ? 'updateContribution' : 'addContribution' }}" class="w-full max-w-lg space-y-5 rounded-2xl border border-stone-200 bg-[#fbf8f2] p-6 shadow-xl"><div><h2 class="text-lg font-semibold">{{ $editingContributionId ? 'Edit contribution' : 'Add Contribution' }}</h2><p class="text-sm text-stone-500">Earmark savings toward this goal.</p></div><label class="block text-sm font-medium">Amount<div class="mt-1 flex"><input type="number" min="0.01" step="0.01" wire:model="amount" class="min-w-0 flex-1 rounded-l-xl border-stone-300"><span class="flex items-center rounded-r-xl border border-l-0 px-3 text-xs">FCFA</span></div>@error('amount')<p class="text-xs text-red-600">{{ $message }}</p>@enderror</label><label class="block text-sm font-medium">Account / Held in <span class="text-xs font-normal text-stone-500">(optional)</span><select wire:model="accountId" class="mt-1 w-full rounded-xl border-stone-300"><option value="">Unassigned</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->name }}</option>@endforeach</select>@error('accountId')<p class="text-xs text-red-600">{{ $message }}</p>@enderror</label><label class="block text-sm font-medium">Date<input type="date" max="{{ now()->toDateString() }}" wire:model="contributedAt" class="mt-1 w-full rounded-xl border-stone-300">@error('contributedAt')<p class="text-xs text-red-600">{{ $message }}</p>@enderror</label><label class="block text-sm font-medium">Note <span class="text-xs font-normal text-stone-500">(optional)</span><textarea wire:model="note" rows="3" class="mt-1 w-full rounded-xl border-stone-300"></textarea></label><div class="flex justify-end gap-2 border-t pt-5"><button type="button" wire:click="$set('showContributionModal',false)" class="rounded-xl border px-4 py-2.5 text-sm font-semibold">Cancel</button><button class="rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white"><span wire:loading.remove>{{ $editingContributionId ? 'Save changes' : 'Add Contribution' }}</span><span wire:loading>Saving...</span></button></div></form></div>@endif
    @if($removingContribution)<div class="fixed inset-0 z-50 flex items-center justify-center bg-stone-950/40 p-4" role="dialog" aria-modal="true"><div class="w-full max-w-md rounded-2xl bg-[#fbf8f2] p-6 shadow-xl"><h2 class="text-lg font-semibold">Remove contribution?</h2><p class="mt-2 text-sm text-stone-600">You're about to remove:</p><div class="mt-4 rounded-xl border p-4"><p class="font-mono font-bold">{{ number_format($removingContribution->amount,0) }} FCFA</p><p class="text-sm">{{ $goal->name }}</p><p class="text-xs text-stone-500">{{ $removingContribution->contributed_at->format('d M Y') }}</p></div><p class="mt-4 text-sm text-stone-600">Your goal progress will be reduced by this amount. Your account balance will not change.</p><div class="mt-6 flex justify-end gap-2"><button type="button" wire:click="$set('confirmingRemovalId',null)" class="rounded-xl border px-4 py-2.5 text-sm font-semibold">Cancel</button><button type="button" wire:click="removeContribution" class="rounded-xl bg-red-700 px-4 py-2.5 text-sm font-semibold text-white">Remove contribution</button></div></div></div>@endif
    @if($confirmingGoalDeletion)<div class="fixed inset-0 z-50 flex items-center justify-center bg-stone-950/40 p-4" role="dialog" aria-modal="true"><div class="w-full max-w-md rounded-2xl bg-[#fbf8f2] p-6 shadow-xl"><h2 class="text-lg font-semibold">Delete goal?</h2><p class="mt-2 text-sm text-stone-600">You're about to permanently delete:</p><div class="mt-4 rounded-xl border p-4"><p class="font-semibold">{{ $goal->name }}</p><p class="mt-2 font-mono text-sm">Saved: {{ number_format($progress['current_amount'],0) }} FCFA</p><p class="font-mono text-sm">Target: {{ number_format($progress['target_amount'],0) }} FCFA</p></div><p class="mt-4 text-sm text-stone-600">This will also remove its contribution history. Your accounts and account balances will not be changed.</p><div class="mt-6 flex justify-end gap-2"><button type="button" wire:click="$set('confirmingGoalDeletion',false)" class="rounded-xl border px-4 py-2.5 text-sm font-semibold">Cancel</button><button type="button" wire:click="deleteGoal" class="rounded-xl bg-red-700 px-4 py-2.5 text-sm font-semibold text-white">Delete goal</button></div></div></div>@endif
</div>
